Feature exploration
===================

- exit code should give more information about build failure (known issue, masked, upstream, restricted download, ...)

- Dark & Light mode colors

- Extend transaction system to use overlayfs or similar (essentially the "session concept" from tinderbox-ng) 

  - different transaction strategies (zfs snapshot, unionfs, overlayfs, fuse, other)

- Create bootstrap/stage-3-9999 overlay system enabling pulling of latest stage

- Portage-ng & Ebuild IPC 

- Reimplement prefix the proper way

- Enable chroot building 
   - prefix style
   - using IPC client

- Think about tinderbox 'statistics' 
   - overlay with templates? 
   - --report showing template based filled with actual data?