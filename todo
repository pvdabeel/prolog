Feature exploration
===================

- standalone loading performance

- Smart QLF split

- Better color handling (fg and bg defined twice in message.pl: once for normal messages and once for bubbles)

- Themeing: Dark & Light mode colors

- test output on default linux and default macos (TERM handling)

- Extend transaction system to use overlayfs or similar (essentially the "session concept" from tinderbox-ng) 

- different transaction strategies (zfs snapshot, unionfs, overlayfs, fuse, snaps, other)

- Create portage-ng ebuild file (overlay?)

- Create portage-ng website

- Building: Clearly define ebuild contract

- Building: Reimplement prefix the proper way

- Building: Ship with own ebuild layer



Performance and readability (2026-07 audit)
===========================================

Measure first (before code changes):

- Baseline Top-N packages with --profile: ctx_union share, dep-model hit rate, reprove counters
  (prover:test_stats_pkgs + stats:test_stats_print_ctx_union_*; A/B config:dep_model_cache)

Tier A performance (only if measured):

- Specialized/indexed feature_unification:unify or narrower heuristic:should_union_ctx
- Ranking overlap precompute + cheaper dep-model choice-sig (keep hazard-encoded cache key)
- prove_recursive ctx-changed path: reuse sorted OldBody; share helpers with prove_model

Tier B performance (if reprove-heavy packages dominate):

- Incremental prune_model / prune_proof / prune_triggers (avoid full AVL rebuild)
- Safer prover:learn store update (nb_setval whole-AVL deep-copy concern


Security (2026-07 full-codebase audit)
======================================

Medium:

- [done] kernelconfig:apply_options — process_create argv (no sh -c); CONFIG suffix gated
- [done] download curl — https-only by default (ftp dropped); config:curl_allow_http for LAN
- [done] sandbox: remove prover:learn/3; post_result requires in-flight job
- [done] kb.qlf / resume.pl / feedback.pl — SHA-256 sidecars (config:file_integrity)
- [done] empty digest password rejected in client/server (require Private/passwords.pl)

Low:

- sanitize docs say shell metacharacters OK for argv; several sh -c sites violate that contract
- Certificates/*.pem public certs tracked (keys gitignored) — speeds attacks if CA key leaks
