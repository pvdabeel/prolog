Feature exploration
===================

- Better color handling (fg and bg defined twice in message.pl: once for normal messages and once for bubbles)

- Themeing: Dark & Light mode colors

- test output on default linux and default macos (TERM)

- Extend transaction system to use overlayfs or similar (essentially the "session concept" from tinderbox-ng) 

  - different transaction strategies (zfs snapshot, unionfs, overlayfs, fuse, other)

- Create portage-ng ebuild
   
- Reimplement prefix the proper way